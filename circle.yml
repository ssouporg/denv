dependencies:
  override:
    - ./circle.sh dependencies
machine:
  pre:
    - denv/circle.sh pre_machine
  services:
    - mongodb
    - docker
  environment:
    MAVEN_OPTS: -Xmx128m
    DOCKER_HOST: 127.0.0.1
    DOCKER_PORT: 2375
test:
  override:
    - mvn clean verify
