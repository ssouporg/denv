dependencies:
  post:
    - mvn clean install -Dmaven.javadoc.skip=true -DskipTests=true
machine:
  services:
    - mongodb
    - docker
  environment:
    MAVEN_OPTS: -Xmx128m
    DOCKER_HOST: tcp://127.0.0.1:2376
test:
  override:
    - mvn clean verify
